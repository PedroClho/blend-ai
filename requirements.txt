# Blend AI — dependências Python
# torch/torchaudio vêm da imagem base; natten, madmom, numpy e cython são
# instalados no Dockerfile (ordem/versões controladas). Ver Dockerfile.

# Áudio / separação / análise
allin1            # beat/downbeat/estrutura COM rótulos (puxa demucs, librosa, hydra, omegaconf, huggingface_hub)
essentia          # estimação de tom (perfil EDMA) — wheel Linux
pyrubberband      # time-stretch / pitch-shift (requer binário rubberband-cli)
soundfile
scipy

# Rekordbox (ground truth de BPM/tom)
pyrekordbox

# Interface
streamlit

# Avaliação / estatística
pandas
scikit-learn
mir_eval          # métricas MIR (beat/downbeat etc.)
museval           # SDR (MUSDB18)

# Dev / testes
pytest
